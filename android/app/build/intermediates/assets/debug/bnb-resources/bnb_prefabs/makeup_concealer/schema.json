{
    "type": "object",
    "description": "Apply concealer.",
    "required": [
        "color",
        "finish",
        "coverage"
    ],
    "properties": {
        "color": {
            "type": "string",
            "default": "0 0 0",
            "description": "Solid color of the region in RGB string format."
        },
        "finish": {
            "type": "string",
            "enum": [
                "natural",
                "matte"
            ],
            "description": "Type of the finish surface."
        },
        "coverage": {
            "type": ["string", "number"],
            "enum": [
                "low",
                "mid",
                "high"
            ],
            "description": "Coverage intensity."
        }
    }
}